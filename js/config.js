// Optional: Toggle simple email mode without backend
// To enable, set window.USE_SIMPLE_EMAIL = true and fill your EmailJS IDs below.
// Get free IDs at https://www.emailjs.com/ (no server needed)

window.USE_SIMPLE_EMAIL = true; // set true to bypass backend and send confirmation email only to the user
window.EMAILJS_PUBLIC_KEY = ""; // e.g. "a1B2c3d4..."
window.EMAILJS_SERVICE_ID = "service_pm3jrvc"; // provided service id
window.EMAILJS_TEMPLATE_ID = "template_mp6qx1m"; // e.g. "template_xxxxx"
